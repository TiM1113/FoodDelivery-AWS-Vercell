{
    "installCommand": "cd .. && pnpm install --frozen-lockfile",
    "buildCommand": "pnpm vercel-build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index"
        }
    ]
}
