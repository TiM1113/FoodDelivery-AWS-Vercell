{
    "installCommand": "cd .. && pnpm install --frozen-lockfile",
    "buildCommand": "pnpm build",
    "outputDirectory": ".",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index.js"
        }
    ]
}
